1 Для вывода данных в консоль в Java используется метод System.out.println(), например:

2 Модификаторы объявления класса в Java могут быть public, private, protected или default (без модификатора). Кроме того, можно использовать модификаторы final, abstract, static.

3 В Java оператор res++ увеличивает значение переменной res на 1, оператор || выполняет логическое ИЛИ, а оператор && выполняет логическое И.

4 Массив в Java представляет собой упорядоченную коллекцию элементов одного типа. Он позволяет хранить несколько значений одного типа под одним именем.

5 Основные конструкции в Java включают в себя условные выражения (if-else), циклы (for, while, do-while), операторы ветвления (switch), операторы присваивания и т.д.